# Telegram Bot для конвертации и анализа переписки

Этот бот принимает ZIP и RAR архивы с HTML-файлами, извлекает их и конвертирует в текстовый формат. Также поддерживается функция анализа переписки с использованием различных LLM моделей через OpenRouter API.

## Требования

- Node.js (версия 14 или выше)
- npm (менеджер пакетов Node.js)
- Аккаунт на [OpenRouter](https://openrouter.ai/) для доступа к LLM моделям (для функции анализа)

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта
2. Откройте терминал и перейдите в директорию проекта
3. Установите необходимые зависимости:

```bash
npm install
```

## Настройка окружения

1. Скопируйте файл `env-example` в `.env`:
```bash
cp env-example .env
```

2. Откройте файл `.env` и введите необходимые значения:
   - Получите API ключ от [OpenRouter](https://openrouter.ai/keys)
   - Укажите ваш токен Telegram бота от @BotFather

```
OPENROUTER_API_KEY=ваш_ключ_openrouter
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
```

## Запуск бота

Запустите бота командой:

```bash
npm run bot
```

После запуска бот будет готов принимать сообщения.

## Возможности

### Конвертация HTML в текст
1. Выберите "📤 Загрузить ZIP или RAR архив"
2. Отправьте ZIP или RAR архив с HTML-файлами
3. Получите результат в виде текстового файла

### Анализ текста с помощью LLM
1. Выберите "📊 Анализ"
2. Загрузите один или несколько текстовых файлов
3. Нажмите "✅ Завершить загрузку"
4. Выберите промпт для анализа или создайте новый
5. Выберите модель LLM для анализа
6. Получите результат в виде HTML-файла

### Управление промптами
1. Выберите "💬 Управление промптами"
2. Создавайте, выбирайте и удаляйте сохраненные промпты

### Выбор модели
1. Выберите "🤖 Выбрать модель"
2. Выберите предпочтительную LLM модель для анализа

## Доступные модели

Бот поддерживает следующие модели через OpenRouter API (цены указаны за 1 миллион токенов):

- Claude 3.7 Sonnet ($3/M)
- GPT-4.1 ($2/M)
- Gemini 2.5 Pro Preview ($1.25/M)
- GPT-4.1 Mini ($0.40/M)
- Llama 4 Maverick ($0.17/M)
- Gemini 2.5 Flash Preview ($0.15/M)
- GPT-4.1 Nano ($0.10/M)
- Gemini 2.0 Flash ($0.10/M)
- Qwen Turbo ($0.05/M)
- Gemini Flash 1.5 ($0.038/M)

## Структура проекта

- `telegram-bot.js` - основной файл бота
- `single-file-converter.js` - скрипт для конвертации HTML в JSON
- `telegram_uploads/` - директория для хранения загруженных архивов
- `telegram_extracted/` - директория для извлеченных файлов
- `telegram_results/` - директория для результатов конвертации
- `telegram_prompts/` - директория для сохраненных промптов
- `telegram_analysis/` - директория для результатов анализа

# Конвертер HTML файлов анализа в текстовый формат

Этот скрипт извлекает данные из HTML файлов анализа и сохраняет их в текстовом формате.

## Требования

Для работы скрипта необходимы следующие библиотеки Python:
- BeautifulSoup4
- pathlib

Установка зависимостей:
```
pip install beautifulsoup4
```

## Использование

1. Поместите все HTML файлы в папку `result`
2. Запустите скрипт:
```
python extract_data.py
```
3. Результаты будут сохранены в папке `extracted_text`

## Структура результата

В текстовых файлах будет следующая информация:
- Метаданные (модель, файл, промпт)
- Дата анализа
- Ответ модели 